enum ViewState { Idle, Busy }

enum ProfileMode { Edit, Own, User }

enum StatusCode {
  informational,
  success,
  redirection,
  clientError,
  serverError
}

enum ContactMethodType { Email, Phone }
